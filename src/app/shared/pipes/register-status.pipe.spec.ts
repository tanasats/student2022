import { RegisterStatusPipe } from './register-status.pipe';

describe('RegisterStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new RegisterStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
