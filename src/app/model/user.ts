export interface User {
        accesstoken?: string,
        username?: string,
        fullname?: string,
        usertype?:string,
        faculty?:string,
        studentcode?:string,
        staffid?:string,
}